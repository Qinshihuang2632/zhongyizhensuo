# 项目协作约定（中医诊所管理系统）

## git 同步规则（用户明确要求）
- **每次 git 同步/推送前，必须先询问用户是否已打开临时代理（7890）**，按回答选择：
  - 已开代理：`git -c http.proxy=http://127.0.0.1:7890 push`
  - 未开代理：可尝试直连（`git -c http.proxy= -c https.proxy= push`），失败则告知用户待网络可用再补推；
- 推送失败的提交留在本地（不丢失），网络恢复后主动补推；
- 服务器程序运行所需数据与 git 完全无关，git 仅用于开发机源码版本管理。

## 构建 / 发布约定
- 日常迭代只重建主程序（`python scripts/build.py --release`）；
  **zip 安装包仅在用户明确要求交付时才打包**（`--installer`）；
- 打包前必须先退出正在运行的系统（build.py 已内置检测并拒绝）；
- 「数据」目录是真实业务数据：更新/安装/打包永不触碰。

## 测试约定
- 每个里程碑/修复都要：后端 API 自测脚本 + 全量回归（tests/ 下各套件）+
  浏览器端到端冒烟（改了哪个页面就冒烟哪个页面）；
- 测试数据用完即清（开发态 `数据/` 目录），用户的 dist 数据是真实数据，绝不能动。

## 用户偏好
- 界面语言为中文，错误提示要具体、面向操作员；
- 打包出的 exe 需在无任何环境的空白 Windows 电脑上双击即用（PyInstaller 绿色版）；
- 打印统一 A4 制式模板，打印前有预览。
